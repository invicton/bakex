AWSTemplateFormatVersion: "2010-09-09"
Description: "BakeX builder onboarding role and EC2 SSM instance profile."

Parameters:
  TrustedPrincipalArn:
    Type: String
    Default: ""
    Description: "Optional IAM principal allowed to assume the BakeX builder role. Leave blank for test stacks to trust this AWS account root; use a specific IAM user or role ARN for production."
    AllowedPattern: "^$|^arn:aws:iam::[0-9]{12}:((user|role)/.+|root)$"
  ExternalId:
    Type: String
    Default: "bakex-onboarding"
    Description: "ExternalId that must also be configured in BakeX. Replace the default with a customer-unique value for production."
    MinLength: 8
  RoleNamePrefix:
    Type: String
    Default: "BakeX"
    Description: "Prefix for IAM roles and instance profile names."
    AllowedPattern: "^[A-Za-z0-9+=,.@_-]{1,32}$"

Conditions:
  UseDefaultTrustedPrincipal: !Equals [!Ref TrustedPrincipalArn, ""]

Resources:
  BakeXBuilderRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${RoleNamePrefix}BuilderRole"
      Description: "Role assumed by BakeX to build hardened golden AMIs."
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              AWS: !If
                - UseDefaultTrustedPrincipal
                - !Sub "arn:aws:iam::${AWS::AccountId}:root"
                - !Ref TrustedPrincipalArn
            Action: sts:AssumeRole
            Condition:
              StringEquals:
                sts:ExternalId: !Ref ExternalId
      Policies:
        - PolicyName: BakeXBuilderPolicy
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: ReadEc2Inventory
                Effect: Allow
                Action:
                  - ec2:DescribeImages
                  - ec2:DescribeInstances
                  - ec2:DescribeInstanceStatus
                  - ec2:DescribeSecurityGroups
                  - ec2:DescribeSubnets
                  - ec2:DescribeVpcs
                  - ec2:DescribeVolumes
                  - ec2:DescribeSnapshots
                  - ec2:DescribeTags
                Resource: "*"
              - Sid: BuildAndCleanTemporaryInstances
                Effect: Allow
                Action:
                  - ec2:RunInstances
                  - ec2:TerminateInstances
                  - ec2:StopInstances
                  - ec2:CreateTags
                  - ec2:CreateImage
                  - ec2:CreateSnapshot
                  - ec2:ModifyImageAttribute
                Resource: "*"
              - Sid: OptionalImageCleanup
                Effect: Allow
                Action:
                  - ec2:DeregisterImage
                  - ec2:DeleteSnapshot
                Resource: "*"
              - Sid: SendBuildCommands
                Effect: Allow
                Action:
                  - ssm:SendCommand
                  - ssm:GetCommandInvocation
                  - ssm:ListCommandInvocations
                  - ssm:DescribeInstanceInformation
                Resource: "*"
              - Sid: PassBakeXInstanceProfile
                Effect: Allow
                Action: iam:PassRole
                Resource: !GetAtt BakeXInstanceRole.Arn
                Condition:
                  StringEquals:
                    iam:PassedToService: ec2.amazonaws.com

  BakeXInstanceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${RoleNamePrefix}BuilderInstanceRole"
      Description: "Instance role for temporary BakeX build instances managed through SSM."
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore

  BakeXInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: !Sub "${RoleNamePrefix}BuilderInstanceProfile"
      Roles:
        - !Ref BakeXInstanceRole

Outputs:
  BakeXRoleArn:
    Description: "Paste this into BakeX as Role ARN."
    Value: !GetAtt BakeXBuilderRole.Arn
  ExternalId:
    Description: "Paste the ExternalId parameter value into BakeX as External ID."
    Value: !Ref ExternalId
  InstanceProfileName:
    Description: "Paste this into BakeX as IAM Instance Profile Name."
    Value: !Ref BakeXInstanceProfile
  RegionHint:
    Description: "Region where this stack was created."
    Value: !Ref AWS::Region
