AWSTemplateFormatVersion: "2010-09-09"
Description: "BakeX scanner onboarding role and EC2 SSM instance profile."

Parameters:
  TrustedPrincipalArn:
    Type: String
    Default: ""
    Description: "Optional IAM principal allowed to assume the BakeX scanner role. Leave blank for test stacks to trust this AWS account root; use a specific IAM user or role ARN for production."
    AllowedPattern: "^$|^arn:aws:iam::[0-9]{12}:((user|role)/.+|root)$"
  ExternalId:
    Type: String
    Default: "bakex-onboarding"
    Description: "ExternalId that must also be configured in BakeX. Replace the default with a customer-unique value for production."
    MinLength: 8
  RoleNamePrefix:
    Type: String
    Default: "BakeX"
    Description: "Prefix for IAM roles and instance profile names."
    AllowedPattern: "^[A-Za-z0-9+=,.@_-]{1,32}$"

Conditions:
  UseDefaultTrustedPrincipal: !Equals [!Ref TrustedPrincipalArn, ""]

Resources:
  BakeXScannerRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${RoleNamePrefix}ScannerRole"
      Description: "Role assumed by BakeX to scan AWS images and instances."
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              AWS: !If
                - UseDefaultTrustedPrincipal
                - !Sub "arn:aws:iam::${AWS::AccountId}:root"
                - !Ref TrustedPrincipalArn
            Action: sts:AssumeRole
            Condition:
              StringEquals:
                sts:ExternalId: !Ref ExternalId
      Policies:
        - PolicyName: BakeXScannerPolicy
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: ReadEc2Inventory
                Effect: Allow
                Action:
                  - ec2:DescribeImages
                  - ec2:DescribeInstances
                  - ec2:DescribeInstanceStatus
                  - ec2:DescribeSecurityGroups
                  - ec2:DescribeSubnets
                  - ec2:DescribeVpcs
                  - ec2:DescribeVolumes
                  - ec2:DescribeTags
                Resource: "*"
              - Sid: LaunchAndCleanTemporaryScanInstances
                Effect: Allow
                Action:
                  - ec2:RunInstances
                  - ec2:TerminateInstances
                  - ec2:CreateTags
                Resource: "*"
              - Sid: SendScanCommands
                Effect: Allow
                Action:
                  - ssm:SendCommand
                  - ssm:GetCommandInvocation
                  - ssm:ListCommandInvocations
                  - ssm:DescribeInstanceInformation
                Resource: "*"
              - Sid: PassBakeXInstanceProfile
                Effect: Allow
                Action: iam:PassRole
                Resource: !GetAtt BakeXInstanceRole.Arn
                Condition:
                  StringEquals:
                    iam:PassedToService: ec2.amazonaws.com

  BakeXInstanceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${RoleNamePrefix}ScannerInstanceRole"
      Description: "Instance role for temporary BakeX scan instances managed through SSM."
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore

  BakeXInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: !Sub "${RoleNamePrefix}ScannerInstanceProfile"
      Roles:
        - !Ref BakeXInstanceRole

Outputs:
  BakeXRoleArn:
    Description: "Paste this into BakeX as Role ARN."
    Value: !GetAtt BakeXScannerRole.Arn
  ExternalId:
    Description: "Paste the ExternalId parameter value into BakeX as External ID."
    Value: !Ref ExternalId
  InstanceProfileName:
    Description: "Paste this into BakeX as IAM Instance Profile Name."
    Value: !Ref BakeXInstanceProfile
  RegionHint:
    Description: "Region where this stack was created."
    Value: !Ref AWS::Region
